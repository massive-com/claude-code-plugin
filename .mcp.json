{
  "mcpServers": {
    "massive": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/massive-com/mcp_massive@v0.8.8",
        "mcp_massive"
      ],
      "env": {
        "MASSIVE_API_KEY": "${user_config.massive_api_key}"
      }
    }
  }
}
